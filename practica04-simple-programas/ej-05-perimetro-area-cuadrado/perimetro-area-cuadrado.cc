#include <iostream>
using  namespace std;

int main () {
    float lado = 0;
    cout << "¿Cuánto mide el lado del cuadrado? ";
    cin >> lado;
    cout << "\n El perímetro mide: " << lado*4 << " y el área mide: " << lado*lado << "\n";
}